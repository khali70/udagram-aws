{
    "scripts": {
        "deploy:front": "cd udagram-frontend && npm install && npm deploy && cd ..",
        "deploy:back": "cd udagram-api && npm install && npm deploy && cd .."
    },
    "devDependencies": {}
}